{
  "name": "lambda",
  "scripts": {
    "build": "esbuild index.ts --platform=node --target=es2020 --outdir=dist --format=esm --bundle --minify --external:@aws-sdk --external:uuid",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.133",
    "esbuild": "^0.20.0"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  }
}
